<!DOCTYPE html>    
<html lang="en">
<%- include( '../partials/head/_head.ejs', { page: 'Public Feed' } ) %> 
<body class="has-background-primary">
<%- include('../partials/navbar/_navbar', { routes } ) %>

  <div class="columns body-columns">
    <div class="column is-half is-offset-one-quarter">
    <% posts.forEach(post => { %>
      <div class="card my-6">
          <div class="header">
              <div class="media">
                  <div class="media-left">
                      <figure class="image is-48x48">
                          <img id="splash" src="<%= post.user.avatar %>" alt="<%= post.user.fullName %>'s avatar' ">
                      </figure>
                  </div>
                  <div class="media-content">
                      <p class="title is-4"><%= post.user.fullName %></p>
                      <p class="subtitle is-6">@<%= post.user.username %></p>
                  </div>
              </div>
          </div>
          <div class="card-image">
              <figure class="image is-4by3">
                  <img src="<%= post.image %>" alt="Placeholder image">
              </figure>
          </div>
          <div class="card-content">
              <div class="level is-mobile">
                  <div class="level-left">
                      <div class="level-item has-text-centered">
                          <a href="">
                              <i class="material-icons">favorite_border</i>
                          </a>
                      </div>
                      
                  </div>
              </div>

              <div class="content">
                  <p>
                      <strong><%= post.bumpCount.length %></strong>
                  </p>
                  <p><%= post.caption %> </p>
                  <br>
                  <time><%= new Date(post.createdAt).toLocaleString() %></time>
              </div>
          </div>
          
      </div>
    <% }) %>
    
  <div class="column is-half is-offset-one-quarter">
<div class="columns body-columns">
</body>
</html>
